name: Windows Compatibility

# Windows compatibility testing — runs fpm against real PyManager on
# multiple Windows versions. Separated from CI (fmt/clippy/unit tests)
# so the main CI stays fast for contributors.
#
# This workflow installs PyManager + Python on each runner, builds fpm,
# and runs the #[ignore] integration tests that require a real `py.exe`.
#
# Future contributors: copy this pattern for linux.yml or macos.yml
# when adding support for other platforms.

on:
  # Run on releases and tags (not every PR — this is heavier)
  release:
    types: [published]
  push:
    tags: ['v*']
  # Manual trigger
  workflow_dispatch:
  # Nightly sanity check
  schedule:
    - cron: '0 6 * * *'

env:
  CARGO_TERM_COLOR: always

jobs:
  compatibility:
    name: Windows ${{ matrix.os-label }}
    runs-on: ${{ matrix.os }}
    continue-on-error: ${{ matrix.experimental || false }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Server 2022 — shares kernel base with Windows 11
          - os: windows-latest
            os-label: 'Server 2022'
          # Server 2019 — shares kernel base with Windows 10 1809
          - os: windows-2019
            os-label: 'Server 2019'

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@1.96.0

      - uses: Swatinem/rust-cache@v2

      - name: Install Python with PyManager from python.org
        shell: pwsh
        run: |
          # GitHub Actions Windows runners have a LEGACY py.exe at C:\Windows\py.exe
          # that does NOT support `py list`, `py install`, etc. It must be removed
          # so the new PyManager (installed as an app execution alias) takes over.
          $legacyPy = "C:\Windows\py.exe"
          if (Test-Path $legacyPy) {
            Write-Host "Removing legacy py.exe at $legacyPy"
            Remove-Item $legacyPy -Force
          }

          # Also remove the legacy launcher DLL if present
          $legacyDll = "C:\Windows\pyshellext.dll"
          if (Test-Path $legacyDll) {
            Remove-Item $legacyDll -Force
          }

          # Install Python 3.14 from the official installer — this includes
          # the new PyManager (py launcher) that supports `py list`, `py install`.
          $installerUrl = "https://www.python.org/ftp/python/3.14.6/python-3.14.6-amd64.exe"
          $installer = "$env:TEMP\python-installer.exe"
          Write-Host "Downloading Python 3.14.6 installer from $installerUrl"
          Invoke-WebRequest -Uri $installerUrl -OutFile $installer -UseBasicParsing

          Write-Host "Installing Python 3.14.6 with PyManager..."
          Start-Process -FilePath $installer -ArgumentList "/quiet", "InstallAllUsers=1", "PrependPath=1", "Include_launcher=1" -Wait

          # Refresh PATH
          $machinePath = [System.Environment]::GetEnvironmentVariable("PATH", "Machine")
          $userPath = [System.Environment]::GetEnvironmentVariable("PATH", "User")
          $env:PATH = "$machinePath;$userPath"

          # The new PyManager installs as an app execution alias at:
          # %LocalAppData%\Microsoft\WindowsApps\py.exe
          # Ensure it's on PATH (it should be via WindowsApps which is in user PATH)
          $windowsApps = "$env:LocalAppData\Microsoft\WindowsApps"
          if (Test-Path "$windowsApps\py.exe") {
            Write-Host "New PyManager found at: $windowsApps\py.exe"
            # Ensure WindowsApps is on PATH
            if ($env:PATH -notlike "*$windowsApps*") {
              $env:PATH = "$windowsApps;$env:PATH"
            }
          }

          # Verify
          $pyCmd = Get-Command py -ErrorAction SilentlyContinue
          if ($pyCmd) {
            Write-Host "py.exe found at: $($pyCmd.Source)"
          }

          py --version
          Write-Host "---"
          py list

      - name: Install additional Python version
        shell: pwsh
        run: |
          # Ensure py launcher is on PATH (refresh after install)
          $machinePath = [System.Environment]::GetEnvironmentVariable("PATH", "Machine")
          $userPath = [System.Environment]::GetEnvironmentVariable("PATH", "User")
          $env:PATH = "$machinePath;$userPath"

          py install 3.13
          py list

      - name: Build fpm
        run: cargo build --release

      - name: Add fpm to PATH
        shell: pwsh
        run: |
          $fpmDir = Join-Path $PWD "target\release"
          echo $fpmDir >> $env:GITHUB_PATH

      - name: Run unit tests
        run: cargo test

      - name: Run integration tests (including ignored)
        shell: pwsh
        run: |
          # Run all tests including #[ignore] ones that need real py.exe
          cargo test -- --ignored --nocapture

      - name: Smoke test — fpm list
        shell: pwsh
        run: fpm list

      - name: Smoke test — fpm current
        shell: pwsh
        run: fpm current

      - name: Smoke test — fpm env
        shell: pwsh
        run: fpm env --shell powershell

      - name: Smoke test — fpm use
        shell: pwsh
        run: |
          fpm env --shell powershell | Out-String | Invoke-Expression
          fpm use 3.13
          fpm current
          python --version

      - name: Smoke test — pass-through
        shell: pwsh
        run: fpm -0p

      - name: Upload test results on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.os-label }}
          path: |
            target/release/fpm.exe
            Cargo.lock