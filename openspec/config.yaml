schema: spec-driven

context: |
  Tech stack: undetected (greenfield project — no source code, package manifests, or build files present)
  Architecture: none (greenfield)
  Testing: none (no test framework or runner detected)
  Style: none (greenfield)
  VCS: not a git repository (git init required before commits/PRs)

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas